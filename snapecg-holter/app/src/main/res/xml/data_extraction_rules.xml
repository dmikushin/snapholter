<?xml version="1.0" encoding="utf-8"?>
<!--
    Modern (Android 12+) replacement for the deprecated allowBackup +
    fullBackupContent attributes. Both clauses below explicitly opt out of
    every storage domain, mirroring `android:allowBackup="false"` on older
    releases. ECG recordings, the holter.db database, and the EncryptedShared
    Preferences pairing-key store are all in `database`/`sharedpref`/`file`
    and must never end up in Auto Backup or device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
